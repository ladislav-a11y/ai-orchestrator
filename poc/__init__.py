"""Top-level namespace for isolated proof-of-concept evaluations.

Nothing under `poc/` is imported by production code (`orchestrator/`) and
nothing in `orchestrator/` is imported here except read-only, by design -
see `poc/hermes_agent/README.md`.
"""
